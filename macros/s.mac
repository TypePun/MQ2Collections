Sub Main()
    /declare a stack
    /call AcceptStack a    
    /return

Sub AcceptStack(stack s)
    /echo 'Stack count: ${s.Count}'
    /echo ${s.Peek}
    /if (!${s.Push[Z]}) {
    	/echo 'Could not Push Z.'
    }
    /return
